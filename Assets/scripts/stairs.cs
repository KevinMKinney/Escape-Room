using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class stairs : MonoBehaviour
{/*
    public int steps;
    public float height;
    public float wigh;
    bool done;

   
    void Update()
    {
        if (done == false)
        {
            done = true;
            for(int i = 0; i < steps; i++)
            {
                transform.Translate(wigh, -height, 0);
                Instantiate(transform).GetComponent<stairs>().enabled = false;
            }

        }
    }*/
}
